# Railway/NIXPACKS configuration
# Installs ffmpeg for video conversion

[nixpkgs]
packages = ["ffmpeg"]
[phases.release]
cmd = "cd volleyball_site && python manage.py migrate --noinput && python manage.py create_test_coach && python manage.py create_admin"