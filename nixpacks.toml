# Railway/NIXPACKS configuration
# This file tells Railway which system packages to install

{ pkgs }:
with pkgs; [
  ffmpeg          # Video conversion library (required for MP4 conversion)
  python314       # Python 3.14 (already installed by default via NIXPACKS)
]
